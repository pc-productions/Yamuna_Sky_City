<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1600 1000" width="1600" height="1000">
  <!-- TEMPORARY 3D-experience preview — replace with a real screenshot of
       the external 3D experience (see content/media.ts explore3dPreview).
       Deliberately neutral: no text baked in; the section's own overlay
       carries the CTA/label. -->
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#172c66"/>
      <stop offset="100%" stop-color="#0a0d16"/>
    </linearGradient>
    <pattern id="grid" width="80" height="80" patternUnits="userSpaceOnUse" patternTransform="skewX(-20)">
      <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#3d5aa8" stroke-width="1" opacity="0.4"/>
    </pattern>
  </defs>
  <rect width="1600" height="1000" fill="url(#g)"/>
  <rect width="1600" height="1000" fill="url(#grid)"/>
  <circle cx="800" cy="520" r="10" fill="#5b78c4"/>
</svg>
